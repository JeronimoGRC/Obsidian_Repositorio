
---

Inicializar la app de `hash-identifier` escribimos el siguiente comando:

```
hash-identifier
```

Herramienta utilizada para identificar el hash que le pasemos al aparecer lo siguiente:

```
--------------------------------------------------
 HASH: 
```

Lo más probable es que el hash correcto sea el primero que nos aparezca como en el siguiente ***ejemplo*** con el siguiente hash:

```
916f4c31aaa35d6b867dae9a7f54270d
```

Salida en la terminal:

```
Possible Hashs:
[+] MD5
[+] Domain Cached Credentials - MD4(MD4(($pass)).(strtolower($username)))
```